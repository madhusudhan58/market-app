name: Market App CI

on:
  push:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Show Files
        run: ls -la

      - name: Build Docker Image
        run: docker build -t market-app .